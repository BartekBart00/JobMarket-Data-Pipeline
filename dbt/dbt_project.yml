name: "data_analytics"
version: "1.0.0"
config-version: 2

profile: "data_analytics"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_analytics:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
