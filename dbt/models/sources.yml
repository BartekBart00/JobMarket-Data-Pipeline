version: 2

sources:
  - name: bronze
    description: "Raw ingested tables (medallion bronze)"
    schema: bronze
    tables:
      - name: raw_offers
        description: "Full job offers"

      - name: raw_offer_unified_title
        description: "Unified job title from LLM enrichment"

  - name: silver
    description: "Tables generated by external scripts (like Python AI mapping)"
    schema: silver
    tables:
      - name: ai_skill_mapping
        description: "AI unified skills"
